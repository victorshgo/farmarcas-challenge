import { Loading as LoadingComponent } from "./styles";

const Loading = () => {
  return (
    <LoadingComponent>
      <div className="loading" />
    </LoadingComponent>
  );
};

export default Loading;
